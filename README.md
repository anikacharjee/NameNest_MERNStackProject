# NameNest_MERNStackProject
NameNest is a lightweight full-stack application built with React and Express that allows users to submit names via a form and store them in a MongoDB database with auto-incremented IDs. This project has "edit" and "delete" functionality too!
